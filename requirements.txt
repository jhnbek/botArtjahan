# Runtime dependencies for Python 3.12.
# This is a bounded dependency specification, not a platform lockfile.
# EasyOCR supplies PyTorch transitively; GPU users should install a compatible
# PyTorch build for their platform first.

easyocr>=1.7,<2
faster-whisper>=1.2,<2
ImageHash>=4.3,<5
numpy>=2.0,<3
openai>=1.30,<3
opencv-python-headless>=4.10,<5
Pillow>=11,<13
pyqtgraph>=0.13,<1
PySide6>=6.8,<7
python-dotenv>=1.0,<2
scenedetect>=0.6,<1
